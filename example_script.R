# Create a numeric vector
numbers <- c(1, 2, 3, 4, 5)

# Calculate the mean of the vector
mean(numbers)

# Plot the vector
plot(numbers, type = "o", col = "blue", pch = 19, main = "Sample Plot", xlab = "Index", ylab = "Values")
